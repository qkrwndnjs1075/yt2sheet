# Score runtime staging fixtures

These tiny executable app trees stand in for the pinned upstream DMGs. The tests
create deterministic cache bytes and use local command fixtures, so no upstream
runtime is downloaded or redistributed during development verification.
